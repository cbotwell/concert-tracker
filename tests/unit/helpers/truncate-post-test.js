import { truncatePost } from '../../../helpers/truncate-post';
import { module, test } from 'qunit';

module('Unit | Helper | truncate post');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = truncatePost(42);
  assert.ok(result);
});
